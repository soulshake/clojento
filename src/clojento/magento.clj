(ns clojento.magento
  (:require [clojento.config :as config]
            [clojento.magento.db :as db]
            [clojure.tools.logging :as log]))

(log/debug "loading clojento.magento namespace")

; (defn load-product [product-id] "TODO")
; (defn load-product-or-variant [product-or-variant-id] "TODO")
